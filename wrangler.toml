name = "saju-site"
compatibility_date = "2024-02-05"

[pages_build_output]
dir = "." # Serve static assets from the root directory
# The _worker.js file in this directory will handle dynamic requests.